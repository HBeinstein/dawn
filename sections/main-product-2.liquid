<div class="page-width page-width--narrow">
  <h2 class="page-title">
    {%- if section.settings.page.title != blank -%}
      {{ section.settings.page.title | escape }}
    {%- endif -%}
  </h2>
  <div class="rte">
    {%- if section.settings.hero-image != blank -%}
      {{ section.settings.hero-image }}
    {%- else -%}
      <div class='page-placeholder-wrapper placeholder'>
        {{ 'page' | placeholder_svg_tag: 'page-placeholder' }}
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Hero Image",
  "settings": [
    {
      "type": "image",
      "id": "hero-image",
      "label": "Hero Image"
    }
  ],
  "name": "Title",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Coffee School"
    }
  ],
  "presets": [
    {
      "name": "Coffee School"
    }
  ]
}
{% endschema %}
